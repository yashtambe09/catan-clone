DATABASE_URL=postgresql://catan:catan@postgres:5432/catan
JWT_SECRET=replace-with-a-long-random-secret
# VITE_BACKEND_URL is intentionally unset here: frontend/src/config.js falls
# back to window.location.hostname, so it works over both localhost and a
# LAN IP without this. Only set it to force a different backend host.
